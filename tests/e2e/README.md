E2E tests

Usage:

- Set `BASE_URL` to the staging URL where the app is running (e.g. https://staging.example.com)
- Run `npm run e2e`

Notes:
- Tests are minimal and assume `schedule_id=1` exists in staging; extend as needed to provision test data.
