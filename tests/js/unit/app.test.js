import { describe, it } from 'vitest'
import '../../../resources/js/app'

describe('app entry', () => {
  it('imports bootstrap without throwing', () => {
    // If import succeeds, test passes
  })
})
